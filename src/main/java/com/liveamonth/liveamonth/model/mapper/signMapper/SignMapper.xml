<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
 
<mapper namespace="com.liveamonth.liveamonth.model.mapper.signMapper.SignMapper">
					
	<!-- insert-->
	<insert id="insertUser" parameterType="com.liveamonth.liveamonth.entity.vo.UserVO">
		insert into User
		values(#{userID},#{userPassword},#{userName},#{userNickname},#{userAge},#{userSex},#{userEmail});
	</insert>


	<!-- select -->
	<select id="checkSign" parameterType="HashMap" resultType="String">
		select userName from User where userID = #{userID} and userPassword =
		#{userPassword}
	</select>

	<select id="checkID" parameterType="String" resultType="String">
		select userID from User where userID = #{userID}
	</select>

	<select id="checkNickName" parameterType="String"
		resultType="String">
		select userNickName from User where userNickName =
		#{userNickName}
	</select>

	<!-- IDFind -->
	<select id="findID" parameterType="String" resultType="String">
		select
		userID from User where userEmail = #{userEmail}
	</select>
</mapper>